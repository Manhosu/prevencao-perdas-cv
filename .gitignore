.venv/
__pycache__/
*.pyc
models/*.pt
models/*_openvino_model/
models/*.onnx
config/config.json
data/
/evidence/
logs/
dev/bin/
dev/videos/
.pytest_cache/
relatorio-capacidade.txt

# artefatos do PyInstaller
/build/
/dist/
*.spec

# instalador compilado (Inno Setup) — 266 MB, entregue por Drive/WeTransfer
/dist_installer/

# vídeos do cliente (ROSTOS REAIS) e material de teste — NUNCA versionar
# (repo é público). Cobre os YK2*.mp4 na raiz e qualquer vídeo baixado.
*.mp4
*.mov
*.avi
/scratchpad_arma/
/scratchpad/
YK2*

# chave privada de licenciamento — NUNCA versionar
*.pem
chave_privada*

# gerador de licenca compilado (ferramenta do revendedor, nunca do cliente)
/dist_gerador/
